# 수정 요청서 — 반응속도 테스트 웹
**작성일:** 2026-04-03  
**프로젝트:** reaction-test-danu-777-v2  
**요청 항목:** 2가지 (구간별 결과 이펙트 / 마이페이지 그래프 개선)

---

## 요청 1. 구간별 결과 이펙트 시스템 추가

### 개요
테스트 결과(ms) 구간에 따라 화면 이펙트, 사운드, 텍스트 멘트를 단계적으로 표시한다.  
결과가 나오는 시점(`result` 상태 진입 시)에 해당 티어 이펙트를 실행한다.

---

### 티어 정의 및 이펙트 스펙

#### Lv.0 — Normal (301ms 이상)
- 화면 이펙트: 없음
- 사운드: 없음
- 멘트 (랜덤 1개 표시):
  ```
  { en: "Not bad",    kr: "평범하지만 괜찮아요" }
  { en: "Keep going", kr: "연습하면 늘어요" }
  { en: "Try again",  kr: "한 번 더 해봐요" }
  ```

---

#### Lv.1 — Good (301ms 초과 ~ 300ms 이하)
- 화면 이펙트: 화면 테두리 초록 flash (지속 0.3초, fade out)
- 사운드: 부드러운 팝 효과음 1회 (Web Audio API — 단순 sine wave 가능)
- 멘트 (랜덤 1개 표시):
  ```
  { en: "Good!",        kr: "평균은 넘었어요" }
  { en: "Nice one",     kr: "나쁘지 않은데요?" }
  { en: "Getting there",kr: "조금만 더 집중해요" }
  { en: "Solid!",       kr: "꽤 안정적이에요" }
  ```

---

#### Lv.2 — Great (251ms ~ 250ms 이하)
- 화면 이펙트: 클릭 지점 기준 파란 ripple 파동 (원형 확산, 0.5초)
- 파티클: 작은 원형 파티클 8개 클릭 지점에서 방사
- 사운드: 경쾌한 2단 상승 톤 효과음
- 텍스트 애니: 멘트 텍스트 슬라이드인 + scale 0.8→1.0
- 멘트 (랜덤 1개 표시):
  ```
  { en: "Great!",       kr: "제법 빠른데요" }
  { en: "Sharp!",       kr: "집중력이 남다르네요" }
  { en: "You're quick", kr: "손이 빠른 편이에요" }
  { en: "On point!",    kr: "상위권이 보여요" }
  ```

---

#### Lv.3 — Excellent (201ms ~ 200ms 이하)
- 화면 이펙트: 화면 전체 노란 flash → 0.4초 fade out
- 파티클: 별 모양 파티클 12개 전방향 방사
- 화면 진동: CSS translate shake 애니 (0.3초, 진폭 4px)
- 사운드: 타악기 느낌 임팩트 효과음
- 텍스트 애니: 바운스 + 잔상(ghost text) 효과
- 멘트 (랜덤 1개 표시):
  ```
  { en: "Excellent!",          kr: "이 정도면 진짜 빠릅니다" }
  { en: "Lightning fast",      kr: "번개 같은 반응이에요" }
  { en: "Are you even human?", kr: "진짜 사람 맞아요?" }
  { en: "Top tier reflexes",   kr: "게이머 반응속도 수준" }
  { en: "Insane focus",        kr: "집중력이 무서운 사람" }
  ```

---

#### Lv.4 — INSANE (151ms ~ 150ms 이하)
- 화면 이펙트: 빨강→주황→노랑 flash 연속 3회
- 파티클: 폭죽 파티클 30개 이상, 전방향 + 중력 적용 (Canvas API 또는 tsParticles)
- 사운드: 폭발 + 환호 복합 효과음
- 텍스트 애니: 무지개 색상 전환 (hue-rotate keyframe)
- 멘트 (랜덤 1개 표시):
  ```
  { en: "INSANE!!",       kr: "이건 말이 안 되는 속도예요" }
  { en: "Superhuman",     kr: "인간의 한계를 넘었습니다" }
  { en: "What the—",      kr: "이게 가능한 일이에요?" }
  { en: "Godlike",        kr: "반응속도 신 강림" }
  { en: "Pro gamer mode", kr: "프로게이머 그 자체" }
  ```

---

#### Lv.5 — LUCKY? (100ms 이하)
- 화면 이펙트: Lv.4와 동일하되 🍀 클로버 파티클 혼합
- 사운드: 슬롯머신 잭팟 효과음
- 텍스트 애니: 멘트 위아래 흔들림 + 물음표 강조
- 특이사항: 이 구간은 "실력"이 아닌 "운" 뉘앙스로 표현
- 멘트 (랜덤 1개 표시):
  ```
  { en: "Wait... really?",  kr: "이거 매크로 아님?" }
  { en: "No way",           kr: "믿기지가 않아요" }
  { en: "Lucky shot?",      kr: "실력인지 운인지 모르겠어요" }
  { en: "Are you a robot?", kr: "진짜로요?" }
  { en: "Hacked.",          kr: "이건 그냥 치트키 수준" }
  ```

---

### 멘트 표시 공통 규칙
- 한글, 영문 상관없이 티어별 배열에서 `Math.random()`으로 매 결과마다 1개 랜덤 선택
- 표시 위치: 결과 박스 상단에 배치.
### 구현 스택 제안
- 파티클: Canvas API 직접 구현 또는 tsParticles
- 사운드: Web Audio API (외부 라이브러리 없이 구현 가능)
- 텍스트/화면 애니: CSS keyframe (@keyframes)
- 화면 진동: `@keyframes shake { 0%,100%{transform:translateX(0)} 25%{transform:translateX(-4px)} 75%{transform:translateX(4px)} }`

---

## 요청 2. 마이페이지 그래프 개선

### 개요
기존 Chart.js 꺾은선 그래프에 페이지네이션과 전체 보기 모드를 추가한다.

---

### 기능 상세

#### 기본 상태: 10개씩 보기 모드
- 진입 시 가장 최근 10개 기록을 기본으로 표시
- 상단 버튼: `10개씩` (활성) / `전체 보기` (비활성) 토글
- 하단 네비게이션:
  - `◀ 이전` / `다음 ▶` 버튼
  - 스크롤 트랙 — 현재 페이지 위치를 thumb으로 표시
  - `1 – 10 / N회` 텍스트로 현재 범위 표시
- 버튼 비활성화: 첫 페이지에서 `이전` disabled, 마지막 페이지에서 `다음` disabled

#### 전체 보기 모드
- 전체 기록을 한 차트에 표시
- Chart.js `autoSkip: true`, `maxTicksLimit: 8` 로 x축 라벨 자동 정리
- 하단 네비게이션 숨김, 대신 `전체 N회 기록` 텍스트 표시

#### 공통 설정
- y축 `reverse: true` — ms 낮을수록 위에 표시 (직관적)
- 툴팁: `Nms` 형식
- 페이지 크기: `const PAGE = 10` 상수로 관리

---

### 데이터 연동
- 기존 Firestore `/users/{uid}/records` 컬렉션 그대로 사용
- 쿼리: `orderBy('createdAt', 'asc')` 로 전체 fetch 후 프론트에서 슬라이싱
- 또는 성능 고려 시 최근 50개만 fetch 후 페이지네이션 처리 (권장)

---

### UI 구조 (컴포넌트 기준)
```
<MyPageGraph>
  ├── 상단: 모드 토글 버튼 (10개씩 / 전체 보기)
  ├── 그래프 영역: <canvas> Chart.js
  ├── 10개씩 모드일 때:
  │    ├── 스크롤 트랙 (thumb 위치 동적 계산)
  │    ├── 범위 텍스트 (1–10 / N회)
  │    └── 이전 / 다음 버튼
  └── 전체 보기 모드일 때:
       └── "전체 N회 기록" 안내 텍스트
```

---

*이 문서는 안티그래비티 에이전트 전달용입니다.*  
*참고 프로젝트: reaction-test-danu-777-v2.web.app*
